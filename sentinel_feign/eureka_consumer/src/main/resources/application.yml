server:
  port: 9012

spring:
  application:
    name: sentinel-fegin-client
  cloud:
    sentinel:
      transport:
        dashboard: localhost:9000
eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:9010/eureka

feign:
  sentinel:
    enabled: true

